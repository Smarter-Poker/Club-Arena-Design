/**
 * Club Arena Design Sandbox — Root App
 * 
 * Minimal shell that renders the design sandbox.
 * Components can be swapped in here for isolated UI development.
 */
export default function App() {
  return (
    <div style={{ padding: '2rem', fontFamily: 'Inter, system-ui, sans-serif' }}>
      <h1 style={{ color: '#e0e0e0', margin: 0 }}>🎰 Club Arena Design Sandbox</h1>
      <p style={{ color: '#888', marginTop: '0.5rem' }}>
        Import and render any component here for isolated design work.
      </p>
    </div>
  );
}
